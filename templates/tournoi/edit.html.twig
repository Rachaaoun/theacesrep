<!DOCTYPE html>

<title>Edit Tournoi</title>

{% block body %}
    <h1>Edit Tournoi</h1>

    {{ include('tournoi/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tournoi_index') }}">back to list</a>

    {{ include('tournoi/_delete_form.html.twig') }}
{% endblock %}
