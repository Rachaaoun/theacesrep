<!DOCTYPE html>

<title>Edit Sponsors</title>

{% block body %}
    <h1>Edit Sponsors</h1>

    {{ include('sponsors/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('sponsors_index') }}">back to list</a>

    {{ include('sponsors/_delete_form.html.twig') }}
{% endblock %}
